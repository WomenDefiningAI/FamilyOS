# last-inbox-check.md

Used by the Household Inbox scheduled task to track when it last ran, so it doesn't reprocess old messages.

---

last_checked: *(agent writes a timestamp here after each run — format: YYYY-MM-DDTHH:MM:SS)*
